package com.tssconsultancy.creational.builder.violation.test;

import com.tssconsultancy.creational.builder.violation.model.Phone;

public class Main {
    public static void main(String[] args) {
//        Phone p1 = new Phone("mac");

        // even if we don't want all attr. we must provide the all attr. to create an object
        // or else we must have to create an constructor having all possible combination of the attrs.
    }
}
